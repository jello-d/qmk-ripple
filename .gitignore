# Local dev context, never in the public repo (linked to the shared-notes repo).
CLAUDE.md
__pycache__/
*.pyc

# Local agent context and state; never part of the public repo.
# CLAUDE.md is a symlink into the private shared-notes repo.
CLAUDE.md
CLAUDE.local.md
.claude/
